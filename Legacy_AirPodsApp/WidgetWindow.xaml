<Window x:Class="AirPodsApp.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AirPods Widget" Height="65" Width="260"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" WindowStartupLocation="Manual"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="#DD18181A" CornerRadius="30" BorderBrush="#555555" BorderThickness="1" Margin="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock x:Name="LeftBatText" Text="L: --" Foreground="#4CAF50" FontWeight="SemiBold" FontSize="14" Margin="10,0"/>
                <TextBlock x:Name="RightBatText" Text="R: --" Foreground="#4CAF50" FontWeight="SemiBold" FontSize="14" Margin="10,0"/>
                <TextBlock x:Name="CaseBatText" Text="C: --" Foreground="#4CAF50" FontWeight="SemiBold" FontSize="14" Margin="10,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="✕" Foreground="#888888" Background="Transparent" BorderThickness="0" 
                    VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0"
                    Click="Close_Click" Cursor="Hand" FontSize="12"/>
        </Grid>
    </Border>
</Window>
